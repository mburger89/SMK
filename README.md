# SMK
A keyboard  firmware written in embedded  swift
currently only for ESP32-C6

it has bluetooth capabilities and the keymap is configure via a json file.
currently only a 5 x 12 keyboard matrix is supported.
