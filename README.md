# SMK
A keyboard  firmware written in embedded  swift
currently only for ESP32-C6

it has bluetooth capabilities and the keymap is configured via a JSON file.
currently supports an (n) number of keys in a 2d keyboard matrix.
